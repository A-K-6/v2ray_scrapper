services:
  v2ray-scrapper:
    build: .
    ports:
      - "${HOST_PORT:-8084}:8084"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
